<?xml version="1.0"?>
<block>
  <name>SU Queued Transmitter</name>
  <key>lsa_su_queued_transmitter_cc</key>
  <category>[lsa]</category>
  <import>import lsa</import>
  <make>lsa.su_queued_transmitter_cc($max_size, $sense_tag,$sense_idx,$accesscode)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Maximum Queue Size</name>
    <key>$max_size</key>
    <value>65535</value>
    <type>int</type>
  </param>
  <param>
    <name>Sensing Info Tag</name>
    <key>$sense_tag</key>
    <value>"sensing_info"</value>
    <type>string</type>
  </param>
  <param>
    <name>Sensing Index Tag</name>
    <key>$sense_idx</key>
    <value>"packet_index"</value>
    <type>string</type>
  </param>
  <param>
    <name>Accesscode</name>
    <key>$accesscode</key>
    <value>1101101101001110</value>
    <type>string</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>byte</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>packet</name>
    <type>byte</type>
  </source>
    <source>
    <name>hdr</name>
    <type>byte</type>
  </source>

</block>
