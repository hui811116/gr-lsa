<?xml version="1.0"?>
<block>
  <name>RX State CTRL</name>
  <key>lsa_pwr_rx_state_ctrl_cc</key>
  <category>[lsa]</category>
  <import>import lsa</import>
  <make>lsa.pwr_rx_state_ctrl_cc($high_db,$low_db)</make>
  <callback>set_threshold($high_db,$low_db)</callback>
  <callback>reset_state($reset)</callback>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  
  <param>
    <name>Threshold High (dB)</name>
    <key>high_db</key>
    <value>-50</value>
    <type>float</type>
  </param>
  <param>
    <name>Threshold Low (dB)</name>
    <key>low_db</key>
    <value>-70</value>
    <type>float</type>
  </param>
  <param>
    <name>Reset</name>
    <key>reset</key>
    <value>False</value>
    <type>bool</type>
  </param>
  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>float</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>fb_out</name>
    <type>message</type>
    <optional>1</optional>
  </source>
</block>
