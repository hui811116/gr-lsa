<?xml version="1.0"?>
<block>
  <name>SU Receiver</name>
  <key>lsa_su_sample_receiver_cb</key>
  <category>[lsa]</category>
  <import>import lsa</import>
  <import>from gnuradio import digital</import>
  <make>lsa.su_sample_receiver_cb($sen_tag,$accesscode,$hdr_const,$pld_const)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Sensing Tag Name</name>
    <key>sen_tag</key>
    <value>sensing</value>
    <type>string</type>
  </param>

  <param>
    <name>Accesscode</name>
    <key>accesscode</key>
    <type>string</type>
  </param>

  <param>
    <name>Header Constellation Object</name>
    <key>hdr_const</key>
    <type>raw</type>
  </param>
  <param>
    <name>Payload Constellation Object</name>
    <key>pld_const</key>
    <type>raw</type>
  </param>
  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>packet</name>
    <type>message</type>
    <optional>1</optional>
  </source>
    <source>
    <name>sensing</name>
    <type>message</type>
    <optional>1</optional>
  </source>
</block>
